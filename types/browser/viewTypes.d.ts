declare namespace ViewTypes {
    type IndexViewParameters = {
        head: string
        title: string
        html: string
        protocol: string
        host: string
        originalUrl: string
        year: number
        version: string
    }
}

export = ViewTypes
